package com.example.pi_ease.Repositories;

import com.example.pi_ease.DAO.Entities.EmailC;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EmailCRepository extends JpaRepository<EmailC,Long> {
}
